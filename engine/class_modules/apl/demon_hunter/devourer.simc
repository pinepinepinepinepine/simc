# Executed before combat begins. Accepts non-harmful actions only.
actions.precombat=snapshot_stats
actions.precombat+=/consume

actions=metamorphosis
actions+=/invoke_external_buff,name=power_infusion,if=!buff.power_infusion.up
actions+=/collapsing_star
actions+=/vengeful_retreat,if=buff.voidstep.up
actions+=/predators_wake
actions+=/the_hunt
actions+=/reapers_toll
actions+=/hungering_slash
actions+=/pierce_the_veil,if=voidsurge_available|talent.duty_eternal&active_enemies=1|talent.hungering_slash
actions+=/voidblade,if=talent.duty_eternal&active_enemies=1|talent.hungering_slash
actions+=/void_ray,if=!buff.eradicate.up
actions+=/eradicate,if=!buff.metamorphosis.up|fury.deficit>=50|!talent.emptiness|action.void_ray.usable_in<=gcd.max|active_enemies>1|buff.voidfall_spending.up
actions+=/soul_immolation,if=refreshable&!buff.metamorphosis.up
actions+=/cull,if=buff.voidfall_spending.up
actions+=/reap,if=soul_fragments>=4&(talent.scythes_embrace|active_enemies<=2)|buff.void_metamorphosis_stack.stack+soul_fragments>=buff.void_metamorphosis_stack.max_stack|buff.voidfall_spending.up
actions+=/devour
actions+=/consume
